/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com','media.istockphoto.com','files.edgestore.dev'], // Add the domain of your image source here
    },
};

export default nextConfig;
